<h1>Instagram</h1>

<% @posts.each do |post| %>
    <%= post.user.user_name %>
    <%= image_tag post.image.url(:medium) %>
    <%= post.caption %>
    <%= link_to 'show', post_path(post) %>
<% end %>